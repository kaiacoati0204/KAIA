# Dependências de PRODUÇÃO — é o que o Render instala.
# Ferramenta de desenvolvimento e teste fica em requirements-dev.txt.

requests
python-dotenv
fastapi
uvicorn
asyncpg
pydantic
apscheduler

# Auth — validação do JWT do Supabase (ES256 via JWKS)
pyjwt[crypto]

# ML — o backend faz pickle.load do RandomForest e do StandardScaler, então
# scikit-learn precisa existir em RUNTIME, não só no treino. Versões fixas de
# propósito: o modelo é regerado no build (ml/gerar_base_v2.py) e um upgrade
# silencioso de major quebraria o unpickle ou o próprio gerador.
numpy==2.2.6
pandas==3.0.3
scikit-learn==1.7.1
