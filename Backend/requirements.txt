# Dependências de PRODUÇÃO — é o que o Render instala.
# Ferramenta de desenvolvimento e teste fica em requirements-dev.txt.

requests
python-dotenv
fastapi
uvicorn
asyncpg
pydantic
apscheduler

# Auth — validação do JWT do Supabase (ES256 via JWKS)
pyjwt[crypto]

# ML — pickle.load do RandomForest/StandardScaler exige scikit-learn em RUNTIME.
# Versões fixas: o modelo é regerado no build (ml/gerar_base_v2.py) e um upgrade
# de major quebraria o unpickle ou o gerador.
numpy==2.2.6
pandas==3.0.3
scikit-learn==1.7.1
